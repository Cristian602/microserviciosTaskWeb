spring.cloud.config.enabled: false
spring.jpa.defer-datasource-initialization: true
server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Cristian602/MicroserviciosTaskWeb.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t

